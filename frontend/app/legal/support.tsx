// SquadPay — Support page (admin-managed via /api/legal/pages/support).
import { LegalPageScreen } from '../../src/components/LegalPageScreen';

export default function SupportScreen() {
  return <LegalPageScreen slug="support" />;
}
