/**
 * adminApi/notifications.ts — Notification matrix admin
 * (Event × Channel push/SMS config).
 */
export { notificationConfigApi } from './_legacy';
export type {
  NotifChannel,
  NotifEventConfig,
  NotificationConfig,
} from './_legacy';
