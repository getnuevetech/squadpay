/**
 * adminApi/reconciliation.ts — PSP reconciliation admin
 * (read-only today; expands when real-time ledger recon is wired).
 */
export type {
  ReconciliationSettings,
  ReconciliationRow,
} from './_legacy';
