/**
 * adminApi/referrals.ts — Referral program admin (settings, stats,
 * referrer detail views).
 */
export type {
  ReferralSettings,
  ReferralStats,
  ReferrerRow,
  ReferrerDetail,
} from './_legacy';
